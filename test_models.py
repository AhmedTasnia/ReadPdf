import google.generativeai as genai
import os

api_key = os.environ.get("GEMINI_API_KEY", "") # I don't have their key, but I might be able to list models without a key? No, needs key.
# I'll just print the help or doc for genai models
